/*
 * webui_stub.c — stand-in for the generated src/webui_embed.c when
 * linking the host smoke binary (the real one is a 4 MiB byte array the
 * smoke test does not need).
 */

const unsigned char webui_index_html_gz[] = { 0 };
const unsigned int  webui_index_html_gz_len = 0;
const int           webui_embed_available = 0;
